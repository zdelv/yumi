#include <stdint.h>

/*  Takes a uint and extracts the last x number of bits
    Input: uint32_t bits, uint32_t extracted_bits, int num_of_bits
    Output: uint32_t extracted_bits
 */

uint32_t extract_last_bits(uint32_t bits, int num_of_bits);
